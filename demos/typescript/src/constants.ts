export const ROUTES = {
  LOGIN: '/',
  HOME: '/home',
  ERROR: '/error',
  NOT_FOUND: '*',
  CALLBACK: '/oauth/callback',
} as const;
export const BACKEND_ENDPOINTS = {
  OAUTH: '/oauth/backend',
} as const;
